<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cm_colorPrimary</item>
        <item name="colorPrimaryDark">@color/cm_colorPrimaryDark</item>
        <item name="colorAccent">@color/cm_colorAccent</item>
    </style>

    <!--日期选择样式-->
    <style name="DatePickerDialog" parent="@android:style/Theme">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>


</resources>
